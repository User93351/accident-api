services:
  - type: web
    name: accident-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: API_KEY
        value: your_secure_api_key_here
      - key: EMAIL_PASSWORD
        value: pptsbbotjoqgwdkp
